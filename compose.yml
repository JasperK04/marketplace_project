services:
  marketplace:
    build: .
    container_name: marketplace
    networks:
      - jasper
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/marketplace.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped

networks:
  jasper:
    external: true